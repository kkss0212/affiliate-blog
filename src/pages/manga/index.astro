---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";

const allEntries = await getCollection("manga", ({ data }) => {
  return import.meta.env.PROD ? data.draft !== true : true;
});
const entries = allEntries.sort((a, b) => a.data.title.localeCompare(b.data.title));
---

<BaseLayout
  title="Manga Guides | Japan Unpacked"
  description="Guides to Japanese manga series and genres, with ways to read."
>
  <h1 class="text-2xl font-semibold">Manga</h1>
  <p class="mt-2 text-slate-600">
    Guides to manga series and genres for people who want to read deeper.
  </p>

  <ul class="mt-6 space-y-3">
    {
      entries.map((entry) => (
        <li>
          <a
            href={`${import.meta.env.BASE_URL}manga/${entry.id}/`}
            class="block rounded-lg border border-slate-200 p-4 hover:border-slate-400"
          >
            <span class="font-medium">{entry.data.title}</span>
          </a>
        </li>
      ))
    }
  </ul>
</BaseLayout>
